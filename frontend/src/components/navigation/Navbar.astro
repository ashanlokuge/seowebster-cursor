---
import ServicesMegaMenu from './ServicesMegaMenu.astro';
import CMSSEOMegaMenu from './CMSSEOMegaMenu.astro';
import IndustrySEOMegaMenu from './IndustrySEOMegaMenu.astro';
// Modern Navbar component for SEOWebster
---

<nav class="sticky top-0 z-50 bg-white shadow-sm border-b border-gray-100 transition-all duration-300" id="navbar">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between h-16">
      <!-- Logo -->
      <div class="flex items-center">
        <a href="/" class="flex-shrink-0">
          <img 
            src="/images/seowebster-logo.svg" 
            alt="SEOWebster - Professional SEO Services" 
            class="h-10 w-auto"
          />
        </a>
      </div>

      <!-- Desktop Navigation -->
      <div class="hidden lg:flex items-center justify-between flex-1 ml-10">
        <!-- Main Navigation Links -->
        <div class="flex items-center space-x-8">
          <a href="/" class="text-gray-700 hover:text-[#ff6200] text-sm font-medium transition-colors duration-200">
            Home
          </a>
          
          <!-- Services Dropdown -->
          <div class="relative group">
            <button class="text-gray-700 hover:text-[#ff6200] text-sm font-medium transition-colors duration-200 flex items-center group-hover:text-[#ff6200]">
              Services
              <svg class="w-4 h-4 ml-1 group-hover:rotate-180 transition-transform duration-200" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
              </svg>
            </button>
            <ServicesMegaMenu />
          </div>
          
          <!-- CMS SEO Dropdown -->
          <div class="relative group">
            <button class="text-gray-700 hover:text-[#ff6200] text-sm font-medium transition-colors duration-200 flex items-center group-hover:text-[#ff6200]">
              CMS SEO
              <svg class="w-4 h-4 ml-1 group-hover:rotate-180 transition-transform duration-200" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
              </svg>
            </button>
            <CMSSEOMegaMenu />
          </div>

          <!-- Industry SEO Dropdown -->
          <IndustrySEOMegaMenu />
          
          <a href="/blog" class="text-gray-700 hover:text-[#ff6200] text-sm font-medium transition-colors duration-200">Blog</a>
        </div>

        <!-- Right Side Actions -->
        <div class="flex items-center space-x-4 xl:space-x-6">
          <div class="hidden xl:flex items-center text-gray-700 hover:text-[#ff6200] text-sm font-medium transition-colors duration-200">
            <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"></path>
            </svg>
            +1 (555) 123-4567
          </div>
          <a href="/login" class="text-gray-700 hover:text-[#ff6200] text-sm font-medium transition-colors duration-200">
            Login
          </a>
          <a href="/contact" class="bg-[#ff6200] hover:bg-[#e55a00] text-white px-4 xl:px-6 py-2 rounded-md text-sm font-medium transition-colors duration-200">
            Start Free Trial
          </a>
        </div>
      </div>

      <!-- Mobile menu button -->
      <div class="lg:hidden flex items-center">
        <button class="text-gray-700 hover:text-[#ff6200] p-2 rounded-md mobile-menu-toggle" id="mobile-menu-button">
          <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
          </svg>
        </button>
      </div>
    </div>

    <!-- Mobile Navigation Menu -->
    <div class="lg:hidden hidden" id="mobile-menu">
      <div class="px-2 pt-2 pb-3 space-y-1 bg-white border-t border-gray-200">
        <a href="/" class="block px-3 py-2 text-base font-medium text-gray-700 hover:text-[#ff6200] hover:bg-gray-50 rounded-md transition-colors duration-200">
          Home
        </a>
        
        <!-- Mobile Services Dropdown -->
        <div class="block">
          <button class="w-full text-left px-3 py-2 text-base font-medium text-gray-700 hover:text-[#ff6200] hover:bg-gray-50 rounded-md transition-colors duration-200 flex items-center justify-between mobile-dropdown-toggle" data-target="mobile-services">
            Services
            <svg class="w-4 h-4 transform transition-transform duration-200" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
            </svg>
          </button>
          <div class="hidden pl-6 space-y-1" id="mobile-services">
            <a href="/services/technical-seo" class="block px-3 py-2 text-sm text-gray-600 hover:text-[#ff6200] rounded-md">Technical SEO</a>
            <a href="/services/on-page-seo" class="block px-3 py-2 text-sm text-gray-600 hover:text-[#ff6200] rounded-md">On-Page SEO</a>
            <a href="/services/local-seo" class="block px-3 py-2 text-sm text-gray-600 hover:text-[#ff6200] rounded-md">Local SEO</a>
            <a href="/services" class="block px-3 py-2 text-sm text-[#ff6200] font-medium rounded-md">View All Services →</a>
          </div>
        </div>
        
        <!-- Mobile CMS SEO Dropdown -->
        <div class="block">
          <button class="w-full text-left px-3 py-2 text-base font-medium text-gray-700 hover:text-[#ff6200] hover:bg-gray-50 rounded-md transition-colors duration-200 flex items-center justify-between mobile-dropdown-toggle" data-target="mobile-cms-seo">
            CMS SEO
            <svg class="w-4 h-4 transform transition-transform duration-200" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
            </svg>
          </button>
          <div class="hidden pl-6 space-y-1" id="mobile-cms-seo">
            <a href="/cms-seo/wordpress-seo" class="block px-3 py-2 text-sm text-gray-600 hover:text-[#ff6200] rounded-md">WordPress SEO</a>
            <a href="/cms-seo/shopify-seo" class="block px-3 py-2 text-sm text-gray-600 hover:text-[#ff6200] rounded-md">Shopify SEO</a>
            <a href="/cms-seo/webflow-seo" class="block px-3 py-2 text-sm text-gray-600 hover:text-[#ff6200] rounded-md">Webflow SEO</a>
            <a href="/cms-seo" class="block px-3 py-2 text-sm text-[#ff6200] font-medium rounded-md">View All CMS →</a>
          </div>
        </div>

        <!-- Mobile Industry SEO Dropdown -->
        <div class="block">
          <button class="w-full text-left px-3 py-2 text-base font-medium text-gray-700 hover:text-[#ff6200] hover:bg-gray-50 rounded-md transition-colors duration-200 flex items-center justify-between mobile-dropdown-toggle" data-target="mobile-industry-seo">
            Industry SEO
            <svg class="w-4 h-4 transform transition-transform duration-200" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
            </svg>
          </button>
          <div class="hidden pl-6 space-y-1" id="mobile-industry-seo">
            <a href="/industry-seo/youtube-seo" class="block px-3 py-2 text-sm text-gray-600 hover:text-[#ff6200] rounded-md">YouTube SEO</a>
            <a href="/industry-seo/mobile-app-seo" class="block px-3 py-2 text-sm text-gray-600 hover:text-[#ff6200] rounded-md">Mobile App SEO</a>
            <a href="/industry-seo/small-business-seo" class="block px-3 py-2 text-sm text-gray-600 hover:text-[#ff6200] rounded-md">Small Business SEO</a>
            <a href="/industry-seo/real-estate-seo" class="block px-3 py-2 text-sm text-gray-600 hover:text-[#ff6200] rounded-md">Real Estate SEO</a>
            <a href="/industry-seo/crypto-seo" class="block px-3 py-2 text-sm text-gray-600 hover:text-[#ff6200] rounded-md">Crypto SEO</a>
            <a href="/industry-seo" class="block px-3 py-2 text-sm text-[#ff6200] font-medium rounded-md">View All Industries →</a>
          </div>
        </div>

        <a href="/blog" class="block px-3 py-2 text-base font-medium text-gray-700 hover:text-[#ff6200] hover:bg-gray-50 rounded-md transition-colors duration-200">
          Blog
        </a>
        <a href="/shop" class="block px-3 py-2 text-base font-medium text-gray-700 hover:text-[#ff6200] hover:bg-gray-50 rounded-md transition-colors duration-200">
          Shop
        </a>
        
        <!-- Mobile Actions -->
        <div class="pt-4 border-t border-gray-200 space-y-2">
          <a href="tel:+15551234567" class="block px-3 py-2 text-base font-medium text-gray-700 hover:text-[#ff6200] hover:bg-gray-50 rounded-md transition-colors duration-200">
            📞 +1 (555) 123-4567
          </a>
          <a href="/login" class="block px-3 py-2 text-base font-medium text-gray-700 hover:text-[#ff6200] hover:bg-gray-50 rounded-md transition-colors duration-200">
            Login
          </a>
          <a href="/contact" class="block mx-3 mt-4 px-4 py-3 bg-[#ff6200] hover:bg-[#e55a00] text-white text-center font-medium rounded-md transition-colors duration-200">
            Start Free Trial
          </a>
        </div>
      </div>
    </div>
    </div>
  </div>
</nav>

<script>
  // Global Mega Menu Manager - prevents conflicts between menus
  interface MegaMenuManager {
    currentMenu: string | null;
    showMenu: (menuName: string) => void;
    hideMenu: (menuName: string) => void;
    hideAllMenus: () => void;
  }

  declare global {
    interface Window {
      MegaMenuManager: MegaMenuManager;
    }
  }

  window.MegaMenuManager = {
    currentMenu: null,
    
    showMenu: function(menuName: string) {
      this.hideAllMenus();
      this.currentMenu = menuName;
      
      // Add a small delay to ensure previous menu is hidden
      setTimeout(() => {
        const menu = document.querySelector(`.${menuName}-mega-menu`);
        const overlay = document.querySelector(`.${menuName}-mega-menu-overlay`);
        
        if (menu) {
          menu.classList.remove('invisible', 'opacity-0');
          menu.classList.add('visible', 'opacity-100');
        }
        if (overlay) {
          overlay.classList.remove('invisible', 'opacity-0');
          overlay.classList.add('visible', 'opacity-100');
        }
      }, 50);
    },
    
    hideMenu: function(menuName: string) {
      if (this.currentMenu === menuName) {
        this.currentMenu = null;
      }
      
      const menu = document.querySelector(`.${menuName}-mega-menu`);
      const overlay = document.querySelector(`.${menuName}-mega-menu-overlay`);
      
      if (menu) {
        menu.classList.add('invisible', 'opacity-0');
        menu.classList.remove('visible', 'opacity-100');
      }
      if (overlay) {
        overlay.classList.add('invisible', 'opacity-0');
        overlay.classList.remove('visible', 'opacity-100');
      }
    },
    
    hideAllMenus: function() {
      const allMenus = document.querySelectorAll('[class*="-mega-menu"]:not([class*="overlay"])');
      const allOverlays = document.querySelectorAll('[class*="-mega-menu-overlay"]');
      
      allMenus.forEach(menu => {
        menu.classList.add('invisible', 'opacity-0');
        menu.classList.remove('visible', 'opacity-100');
      });
      
      allOverlays.forEach(overlay => {
        overlay.classList.add('invisible', 'opacity-0');
        overlay.classList.remove('visible', 'opacity-100');
      });
      
      this.currentMenu = null;
    }
  };

  // Mobile menu functionality
  document.addEventListener('DOMContentLoaded', function() {
    const mobileMenuButton = document.getElementById('mobile-menu-button');
    const mobileMenu = document.getElementById('mobile-menu');
    const mobileDropdownToggles = document.querySelectorAll('.mobile-dropdown-toggle');
    const navbar = document.getElementById('navbar');

    // Toggle mobile menu
    if (mobileMenuButton && mobileMenu) {
      mobileMenuButton.addEventListener('click', function() {
        mobileMenu.classList.toggle('hidden');
        
        // Toggle hamburger icon
        const svg = this.querySelector('svg');
        if (svg) {
          if (mobileMenu.classList.contains('hidden')) {
            svg.innerHTML = '<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>';
          } else {
            svg.innerHTML = '<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>';
          }
        }
      });
    }

    // Mobile dropdown functionality
    mobileDropdownToggles.forEach(toggle => {
              toggle.addEventListener('click', function(this: HTMLElement) {
          const targetId = this.getAttribute('data-target');
          if (targetId) {
            const targetMenu = document.getElementById(targetId);
            const arrow = this.querySelector('svg');
            
            if (targetMenu && arrow) {
              targetMenu.classList.toggle('hidden');
              arrow.classList.toggle('rotate-180');
            }
          }
        });
    });

    // Close mobile menu when clicking outside
    document.addEventListener('click', function(event) {
      if (mobileMenu && !mobileMenu.contains(event.target as Node) && !mobileMenuButton?.contains(event.target as Node)) {
        mobileMenu.classList.add('hidden');
        const svg = mobileMenuButton?.querySelector('svg');
        if (svg) {
          svg.innerHTML = '<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>';
        }
      }
    });

    // Sticky navbar scroll effect
    window.addEventListener('scroll', function() {
      const scrollTop = window.pageYOffset || document.documentElement.scrollTop;
      
      if (navbar) {
        // Add enhanced shadow when scrolled
        if (scrollTop > 10) {
          navbar.classList.add('shadow-lg');
          navbar.classList.remove('shadow-sm');
        } else {
          navbar.classList.remove('shadow-lg');
          navbar.classList.add('shadow-sm');
        }
      }
    });
  });
</script> 