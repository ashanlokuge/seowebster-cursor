---
const pricingPlans = [
  {
    name: "Starter Plan AU",
    price: "AUD $899",
    period: "/month",
    description: "Perfect for small Australian businesses",
    features: [
      "10 Keywords Optimized (AU focused)",
      "Monthly SEO Audit",
      "Basic Local SEO Australia",
      "Email Support in English"
    ],
    buttonText: "Get Started",
    buttonLink: "/au/contact",
    popular: false,
    gradient: "from-blue-500 to-cyan-500"
  },
  {
    name: "Growth Plan AU",
    price: "AUD $1,799",
    period: "/month",
    description: "Designed for growing Australian businesses",
    features: [
      "25 Australian Keywords Targeted",
      "Technical SEO Fixes",
      "Local Citations & GMB AU",
      "Monthly Reporting Call"
    ],
    buttonText: "Start Free Audit",
    buttonLink: "/au/contact",
    popular: true,
    gradient: "from-orange-500 to-red-500"
  },
  {
    name: "Enterprise Plan AU",
    price: "Custom Quote",
    period: "",
    description: "Scalable solutions for large Australian enterprises",
    features: [
      "Unlimited Australian Keywords",
      "Dedicated SEO Manager",
      "Multi-City Optimization",
      "Full Technical Audit & Support"
    ],
    buttonText: "Contact Sales",
    buttonLink: "/au/contact",
    popular: false,
    gradient: "from-purple-500 to-pink-500"
  }
];
---

<section class="relative bg-gradient-to-br from-gray-50 to-white py-16 lg:py-24 overflow-hidden">
  <!-- Background Pattern -->
  <div class="absolute inset-0 opacity-5">
    <svg class="absolute inset-0 w-full h-full" xmlns="http://www.w3.org/2000/svg">
      <defs>
        <pattern id="pricing-pattern-au" x="0" y="0" width="60" height="60" patternUnits="userSpaceOnUse">
          <circle cx="30" cy="30" r="2" fill="#00843D"/>
          <circle cx="10" cy="10" r="1" fill="#FFD100"/>
          <circle cx="50" cy="50" r="1" fill="#FFD100"/>
        </pattern>
      </defs>
      <rect width="100%" height="100%" fill="url(#pricing-pattern-au)"/>
    </svg>
  </div>

  <div class="relative z-10 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <!-- Header -->
    <div class="text-center mb-12 lg:mb-16">
      <span class="inline-flex items-center px-3 py-1 rounded-full text-sm font-medium bg-green-50 text-green-600 mb-4">
        Australian Pricing
      </span>
      <h2 class="text-3xl lg:text-4xl xl:text-5xl font-bold text-gray-900 mb-4 leading-tight">
        Simple, Transparent SEO Pricing for Australia
      </h2>
      <p class="text-lg lg:text-xl text-gray-600 max-w-3xl mx-auto leading-relaxed">
        Choose the plan that fits your Australian business growth stage. No hidden fees — just results that drive your success down under.
      </p>
    </div>

    <!-- Pricing Cards -->
    <div class="grid grid-cols-1 md:grid-cols-3 gap-6 md:gap-8">
      {pricingPlans.map((plan) => (
        <div class={`relative group ${plan.popular ? 'lg:scale-105' : ''}`}>
          {plan.popular && (
            <div class="absolute -top-4 left-1/2 transform -translate-x-1/2 z-20">
              <div class="bg-gradient-to-r from-orange-500 to-red-500 text-white px-4 py-2 rounded-full text-sm font-semibold shadow-lg">
                Most Popular in AU
              </div>
            </div>
          )}
          
          <div class={`relative h-full bg-white rounded-2xl p-8 lg:p-10 shadow-lg hover:shadow-2xl transition-all duration-500 border-2 ${
            plan.popular 
              ? 'border-orange-200 shadow-orange-100/50' 
              : 'border-gray-100 hover:border-gray-200'
          } group-hover:-translate-y-2`}>
            
            <!-- Plan Header -->
            <div class="text-center mb-6">
              <h3 class="text-2xl font-bold text-gray-900 mb-2">{plan.name}</h3>
              <p class="text-gray-600 text-sm">{plan.description}</p>
            </div>

            <!-- Pricing -->
            <div class="text-center mb-8">
              <div class="flex items-baseline justify-center">
                <span class="text-4xl lg:text-5xl font-bold text-gray-900">{plan.price}</span>
                {plan.period && <span class="text-lg text-gray-500 ml-1">{plan.period}</span>}
              </div>
              {plan.price.includes('AUD') && (
                <p class="text-sm text-gray-500 mt-2">🇦🇺 Australian Dollar pricing</p>
              )}
            </div>

            <!-- Features -->
            <div class="space-y-4 mb-8">
              {plan.features.map((feature) => (
                <div class="flex items-start">
                  <div class="flex-shrink-0 w-5 h-5 bg-gradient-to-br from-green-400 to-green-500 rounded-full flex items-center justify-center mr-3 mt-0.5">
                    <svg class="w-3 h-3 text-white" fill="currentColor" viewBox="0 0 20 20">
                      <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd"></path>
                    </svg>
                  </div>
                  <span class="text-gray-700 text-sm lg:text-base">{feature}</span>
                </div>
              ))}
            </div>

            <!-- CTA Button -->
            <div class="text-center">
              <a 
                href={plan.buttonLink}
                class={`inline-flex items-center justify-center w-full px-6 py-4 text-base font-semibold text-white rounded-xl transition-all duration-300 transform hover:scale-105 ${
                  plan.popular 
                    ? 'bg-gradient-to-r from-orange-500 to-red-500 hover:from-orange-600 hover:to-red-600 shadow-lg hover:shadow-xl' 
                    : 'bg-gradient-to-r from-gray-800 to-gray-900 hover:from-gray-900 hover:to-black shadow-md hover:shadow-lg'
                }`}
              >
                {plan.buttonText}
                <svg class="w-4 h-4 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                </svg>
              </a>
            </div>

            {/* Australian Guarantee Badge for popular plan */}
            {plan.popular && (
              <div class="absolute -bottom-6 left-1/2 transform -translate-x-1/2">
                <div class="bg-green-500 text-white px-4 py-2 rounded-full text-xs font-semibold shadow-lg">
                  🇦🇺 Australian Business Guarantee
                </div>
              </div>
            )}
          </div>
        </div>
      ))}
    </div>

    <!-- Additional Info -->
    <div class="text-center mt-12 lg:mt-16">
      <div class="bg-gradient-to-r from-green-50 to-blue-50 rounded-2xl p-8 lg:p-10 border border-green-100">
        <h3 class="text-2xl lg:text-3xl font-bold text-gray-900 mb-4">
          Why Australian Businesses Choose SEOWebster
        </h3>
        <p class="text-gray-600 text-lg mb-6 max-w-2xl mx-auto">
          We understand the unique challenges of the Australian market, from local search behavior to competition across major cities like Sydney, Melbourne, and Brisbane.
        </p>
        <div class="grid grid-cols-1 md:grid-cols-3 gap-6 max-w-4xl mx-auto">
          <div class="text-center">
            <div class="w-12 h-12 bg-green-100 rounded-lg flex items-center justify-center mx-auto mb-3">
              <svg class="w-6 h-6 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
              </svg>
            </div>
            <h4 class="font-semibold text-gray-900 mb-2">AU Market Expertise</h4>
            <p class="text-gray-600 text-sm">Deep understanding of Australian search behavior and local SEO strategies.</p>
          </div>
          <div class="text-center">
            <div class="w-12 h-12 bg-blue-100 rounded-lg flex items-center justify-center mx-auto mb-3">
              <svg class="w-6 h-6 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path>
              </svg>
            </div>
            <h4 class="font-semibold text-gray-900 mb-2">Fast Results</h4>
            <p class="text-gray-600 text-sm">See improvements in your Australian search rankings within 60-90 days.</p>
          </div>
          <div class="text-center">
            <div class="w-12 h-12 bg-yellow-100 rounded-lg flex items-center justify-center mx-auto mb-3">
              <svg class="w-6 h-6 text-yellow-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1"></path>
              </svg>
            </div>
            <h4 class="font-semibold text-gray-900 mb-2">Transparent Pricing</h4>
            <p class="text-gray-600 text-sm">Clear AUD pricing with no hidden fees or surprise charges.</p>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
